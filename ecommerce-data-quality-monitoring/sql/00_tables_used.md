# Tables used (anonymized)

This project was originally implemented on a production e-commerce database.
Table names and identifiers were anonymized.

- `catalog_products` represents the core products table (e.g., a Spree-like product model).
- `catalog_products_snapshot` represents an enriched/snapshotted layer used for monitoring and validation.
